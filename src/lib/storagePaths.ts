/** @deprecated Nutze `assetUrls` — Re-Export für bestehende Imports. */
export {
  collectStoragePaths,
  getSupabaseStoragePathFromUrl as getStoragePathFromPublicUrl,
  STORAGE_BUCKET,
} from './assetUrls'
